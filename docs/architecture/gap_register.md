# Gap Register

| ID | Gap | Impact | Required Artifact |
| --- | --- | --- | --- |

Current open gaps: `none`.

Last closed wave:
- slices `101-106` restored direct root Python rerun parity against legacy MATLAB outputs for `THORP`, `GOSM`, and `TDGM`, closed the remaining root `GOSM` rerun warning gap, and left reproducible `PNG + CSV` Plotkit comparison bundles under `out/rerun_parity/`
