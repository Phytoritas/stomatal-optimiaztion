## Why
- The root MATLAB kernel parity wave is closed, but the original THORP, GOSM, and TDGM example and figure workflows remain outside the migrated repo.
- The user reopened the program with an explicit workflow-reproduction goal, so the architecture needs a new audit note and open gaps for example parity.
- The next code slice should be chosen against a documented example inventory rather than reopening the model-kernel audit.

## Scope
- audit legacy example and figure assets for root `THORP`, `GOSM`, and `TDGM`
- define the new parity rule for Plotkit-style publication rendering
- reopen the gap register only for bounded example and figure workflows

## Validation
- verify the inventory directly against `C:\\Users\\yhmoo\\OneDrive\\Phytoritas\\00. Stomatal Optimization`
- update `gap_register.md`, `Phytoritas.md`, and `README.md` so the next example slice is explicit

## Comparison target
- `THORP/example/THORP_code_forcing_outputs_plotting/`
- `GOSM/example/`
- `TDGM/example/Supplementary Code __ TDGM Offline Simulations/`
- `TDGM/example/Supplementary Code __THORP_code_v1.4/`
